# Markdown 全格式测试文档

> 用于 WebUI 画布 / 标题树 / 批注 / 表格 回归。  
> 打开路径见文末说明。

前言段落（标题前的正文）。支持 **粗体**、*斜体*、***粗斜体***、~~删除线~~、行内 `code`，以及 [链接示例](https://example.com)。

---

## 1. 标题层级

### 1.1 三级标题 H3

#### 1.1.1 四级标题 H4

##### 五级 H5

###### 六级 H6

正文：各级标题应在标题树中出现，画布上字号与颜色按层级区分。

## 2. 段落与行内样式

普通段落无法收纳，按正文显示。

同一段落内混排：**加粗**、*倾斜*、***粗斜***、~~划掉~~、`const x = 1`、[外部链接](https://maou.local)。

连续第二段。中间空一行。

## 3. 引用块

> 这是一段引用。
> 可以多行。
>
> 引用里也可以有 **强调** 和 `code`。

## 4. 无序列表（大纲树节点）

- 层级一 A
  - 里面的内容 A1
  - 里面的内容 A2
    - 更深一层 A2-1
- 层级一 B
  - 里面的内容
    1. ~~删除掉~~
    2. *斜体显示内容*
    3. **粗体显示内容**
    4. ***粗斜体***
    5. 行内 `snippet`

## 5. 有序列表

1. 第一步：打开文件
2. 第二步：点标题树
3. 第三步：在画布批注
   1. 子步骤 3.1
   2. 子步骤 3.2

## 6. 任务列表（Todo）

- [ ] todo 任务1
    - [ ] 子任务1
    - [ ] 子任务2
- [ ] todo 任务2
  - [ ] 子任务3
- [x] 已完成任务（应显示勾选）
  - [x] 已完成子任务

### 6.1 验收标准示例

- [ ] 正确密码进入首页
- [ ] 错误密码有提示
- [x] 登录页 UI 定稿

## 7. 代码块

### 7.1 无语言标记（默认折叠）

```
这是一个代码块
多行文本
  缩进保留
```

### 7.2 TypeScript

```ts
export function greet(name: string): string {
  return `Hello, ${name}!`;
}

// 注释与字符串
const n = 42;
```

### 7.3 JSON / Shell

```json
{
  "ok": true,
  "items": [1, 2, 3]
}
```

```bash
pnpm --filter @little-house-studio/webui build
curl -s http://127.0.0.1:8787/api/health
```

## 8. 表格

| 列 A | 列 B | 列 C |
| --- | --- | --- |
| 单元格 1 | **加粗格** | `code` |
| 第二行 | 普通 | 链接见段落 |
| 第三行 | 可编辑测 | 试增删行列 |

| 短表 |
| --- |
| only one column |

## 9. 分割线

上文。

---

下文（分割线两侧应分开）。

## 10. 混合长文结构

### 10.1 功能说明

列表 + 段落 + 代码：

- 入口：`Markdown` 标签
- 对齐：顶栏按钮

```text
把[webui/docs/md-kitchen-sink.md里面的（12）'示例原文']细化这里
```

### 10.2 引用 + 任务

> 风险：表格在窄屏下需横向滚动。

- [ ] 验证悬浮气泡在卡片正上方
- [ ] 验证布局全宽无右半空白

## 11. 边缘情况

空列表项旁的普通行：

- item
  续行若被解析为子内容，视实现而定

仅空格行之后的段落。

符号：`& < > " '` 与中文标点「」『』。

很长一行用来测换行：这是一段故意写得很长很长很长的文字用来检查画布与卡片在窄列下是否会撑破布局以及省略号或换行是否正常工作ABCDEFG_0123456789。

## 12. 需求元数据标题（可选识别）

### [P0] REQ-DEMO-001 示例需求 [WIP] #demo

- [ ] 验收项 A
- [ ] 验收项 B

### [P1] REQ-DEMO-002 次要需求 [TODO]

说明段落。

## 问题待办（H1 同级）

用于测标题树多个 H1/H2。

### 子问题

- [ ] 勾选联动子任务

---

**文件结束。** 若某块未渲染，请对照 `markdown/parser` 与画布组件查缺。
