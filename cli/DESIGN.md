# Maou CLI 

## 视觉风格
- 配色：
  - 主色：黑色
  - 辅助色：荧光黄绿、危险橙红、警告黄、浅罗兰紫
- UI风格：
  - 沉浸式
  - 必要ascii图标
  - 用背景色分块
  - 信息密集，分割线距离小

## agent列表界面
- 输入框空白的时候，点击←键进入agent列表界面，→键返回聊天界面
- 正在运行的agent列表与状态
  - 项目名称
  - main agent
  - 子agent
- 上下选择，点击enter进入选中agent的聊天界面


## 聊天界面
- 使用弹窗（不显示背景，任意位置）
  - 会话记录选择
- 收纳栏（组件）
- 选择菜单（组件）
  - 指令补全、模型选择等等
- 状态栏（组件，最底部位置）
- 输入框（组件，最底部固定第二位置）
- 上下文窗口（组件，居中）
- 左可收纳侧边栏（组件）
  - 暂时无内容
- 右可收纳侧边栏，cli拓展显示组件栏（组件）
  - 暂时无内容

- 空间分布，除此之外没别的了：
[左侧边栏（默认收纳）][上下文窗口][右输入栏(默认收纳)]
[输入框（固定底部）]
[状态栏/功能菜单弹窗（固定底部）]

## 上下文窗口（对话区）
- 整个界面95%面积应该沉浸式，是最重要的区域
- 可以滚轮滑动窗口
- 角色内容（背景块显示颜色，例如user、assistant、system）
  - 卡片渲染
    - 代码气泡，全彩显示
    - md渲染，
    - 工具卡片折叠与展开
- 显示的内容：
  - content内容：完整显示
    - md格式、代码结构渲染
  - 工具调用部分：
    - 工具卡片：每个工具都是一个收纳卡片
      - 创建文本、编辑：显示附近10行，收纳
      - 阅读工具、仅仅显示工具调用记录，默认收纳

# 固定下对齐
- 下面为一个布局块，为一个整体区域，常驻标准最底部对齐的区域，比上面优先显示
## 事件块
- 这里显示展示给用户的事件
- ai处理中：显示当前轮上传和下传的token数量，ai思考中、生成中等等

## 输入框
- 最底部往上数固定第二位置，状态栏上面
- 就一个框，没文字的时候显示灰色的"输入文字"。
- 默认一行，自动根据文本内容自适应改高度（自适应）
  - 默认一行时候滚轮滑动没有操作，超过4行开启滚轮滑动
  - 最高4行
  - 可以超过4行可以鼠标滑动
  - 使用ctrl+e变成全屏文字编辑器界面（支持md渲染）
    - esc退出编辑器回到原界面
    - 回车不发送，变成回车换行，退出全屏编辑的enter才会发送
    - 可以滚轮滑动
- 使用具有自动补全功能
  - 指令补全
- 支持鼠标点击定位到输入框位置，支持拖拽选择文字

- 按键
  - alt+enter换行
  - enter发送指令
  - alt+退格，快速文字退格
  - shift+点击文字选中区域文字
  - 粘贴上去过长的内容会被占位符替换显示（实际上发送出去是完整内容非占位符），删除占位符是这一整块占位符删除掉。
-

## 状态栏/功能菜单
- 自动布局
- 如果需要就变成功能菜单存在
- 默认状态下
  - 从左往右
    - 当前agent状态
    - 后台终端数
    - 上下文占用量
      -  [上下文占用token/模型总token][变色条形显示][占用率][最近20轮平均缓存率]

# 悬浮窗（显示优先级最高）
## 悬浮菜单弹窗
- ctrl+k 打开悬浮的功能弹窗，esc/ctrl+k取消/返回，回车打开选项子菜单


# 问题待办
- [x] 输入框有文字的时候按左右上下应该可以移动光标位置，没文字的时候按下左键应该可以进入到agnet管理界面
  - [x] Agent管理界面（AgentPanel：空输入框 ← 进入，列出 main/子agent，Enter 切换 main 为新会话）
- [x] 输入框光标在非最顶部的时候，点击上下方向键应该可以移动光标位置，再点击上键时会到光标最左上角，再点击上会回去之前发送的指令，最高支持20个指令。下键也同理。
- [x] 修复我没有在最底部却也会跟着生成信息移动的问题（!autoFollow 时 offset 随 maxChatScroll 同步增加，视口钉住）
- [x] 把上下文的排版更清晰可视化，去掉不合理的排版信息（分隔线响应式 hr(cols)、user 消息 wrap、preview 截断）
- [x] 时不时会闪烁但不知道什么原因，不是很严重。（spinner 局部化到 MessageRow/ToolCard，移除全 App 200ms frame；ScrollHistory 测量守卫）
- [x] 点击卡片可以展开（已经达成），但我希望保持点击的卡片头的位置不偏移，而是信息往下展开。（ToolCard 测高 + expandShift 同步 offset，marginTop 不变）
- [x] 鼠标还是无法选择文字，或者选择了我不知道？我没看见反色。（Ink 无法自绘反色选区，改终端原生：Shift+拖拽 / iTerm2 Option+拖拽，首次点击 toast 提示）
- [x] 光标点击输入框文字位置无法移动光标，点击上下左右也无法移动光标。把移动光标这一块根据我的设计+你认为最好的设计去做。
- [x] 打字的时候输入的文字，它不会越来越往后，而是往前，在开头一直插入。我输入到后面的文字，它会插入到开头。（补全状态提升到 store，acceptCompletion 用 sel.value 替换）
- [x] ctrl+c第一次警告，3秒内第二次退出（streaming 时第一次中断）
