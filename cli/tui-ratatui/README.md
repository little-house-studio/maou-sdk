# maou-tui-ratatui

唯一产品 TUI 后端。

需要 **rustc ≥ 1.88**。更旧的 rustc 请走预编译（`node scripts/ensure-maou-tui.mjs`）。

```bash
cd maou-sdk/cli && npm run build:tui-ratatui
```

产物安装到 `~/.maou/bin/maou-tui-ratatui`（Windows `.exe`）。
