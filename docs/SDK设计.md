- llm层级（这里处理所有llm api的原始信息和流式合成，以及关键字解析等等）
    - llm通信协议转换层
    - llm发送层
    - llm流式合成
    - json关键字解析层(包含流式和非流式)
    - json修复与json校验（修复无效json、schema校验）
    - 协议路由分发（根据协议名分派到对应适配器）
    - llm配置层（包含llm api的key，model，temperature等等，以及llm厂商预设，自定义预设等等功能接口）
    - 事件hook与流式事件抽象（首字、思考开始/结束、输出开始/结束、解析错误等）
    - 附件与多模态支持（图片、视频、音频、文档）
    - 速率限制与并发控制（多agent同时调LLM时的排队与限流）
    - 关键词识别阻止信息（例如敏感词、违规内容、广告等）
    - 循环检测器（防agent死循环，检测重复工具调用模式）
- 工具层（agent层级相互依赖）
    - 终端（包含黑名单白名单安全机制，终端持久化一次性管理等等这个是很大的内容）
        - 安全审批机制（敏感操作需用户确认，如删文件、执行危险命令）
    - read、write、edit
    - grep、glob（文件搜索）
    - 工具执行沙箱（限制文件系统访问路径、网络访问域名）
    - 实例化与注册SDK设计
- 上下文层
    - 上下文增量与记忆系统
    - 上下文压缩系统
    - 所有上下文操作
    - 记录与raw存储
    - 长期记忆持久化与检索（跨session的用户偏好、项目知识、自动摘要归档）
- agent层（这里有最小的agent系统层）
    - 上下文操作（例如读取、删除，新上下文，对话增量，压缩，以及一些附加）
    - 提示词与模板解析系统
    - agent-loop接口（条件与调用）
    - 烘培与增量注入系统
    - 流式工具调用与结构化输出实时检测（tool name + 参数流式解析）
    - 会话管理（创建/切换/fork/清空会话）
    - token用量追踪与费用计算（分钟级记录、缓存命中率、价格统计）
    - 其他暴露的接口与功能与事件hook等等
- herness层（这里是maou的基本herness应用设计，也是最终的完整agent产品的内容，是maou设计哲学和产品功能的核心）
    - fork与合并与销毁克隆体（复制上下文和agent实例化加载上下文，合并上下文是信息插入）
    - 文件锁（防止多个agent同时操作同一个文件）
    - team团队互通功能（使用agent信息事件+工具，带公告栏）
        - agent间消息路由（A的输出是B的输入，agent间数据传递）  
    - plan模式（回合注入提示词，让ai先问问题，清楚了再task，没问题就执行开始loop）
    - task功能（task表接管agentloop条件，获取路线与状态并行克隆分配任务完成汇报成功后销毁）
        - 任务调度与并发控制（多线克隆agent并行任务分配）
    - board功能（烘焙注入内容）
    - notebook功能（消息前置注入）
    - 待办栏（用户写的待办任务，ai可以自己领取任务完成）
    - goal功能（用另一个agent去做需求监管）
    - skill
    - 文件操作
    - 文件搜索
    - 用户反馈工具
    - 网络搜索
    - 浏览器操作
    - 终端后台系统与管理
    - subagent（子agent调用工具）
    - task-finish（任务完成标记工具）
    - load-skill（skill加载与注册工具）
    - project-manage（项目管理工具）
    - fun工具（趣味互动 so_cool/so_bad）
    - harness事件总线（plan/task/goal变更通知、组件间解耦通信）
    - 外部集成与通知推送（webhook、飞书推送、桌面通知）
    - GLOBAL-agent团队设计（在全局环境里面的agent团队预设，包含上下文记忆系统预设，可用工具，团队标准等等）
    - PROJECT-agent团队设计（在项目环境里面的agent团队预设，包含上下文记忆系统预设，可用工具，团队成员标准等等）
    - 其他设计：意图识别、agent自发性行为
- agent/herness管理层（这里管理agent和herness层的交互和在应用中的管理）
    - 插件系统（PluginBase生命周期钩子、插件发现与热加载）
    - 把agent模板实例化与注册成agent
    - agent管理（包含agent的启动、停止、重启、删除等等操作）
    - agent状态获取（包含agent的状态，上下文，任务，待办栏，团队等等）
    - 进入控制某个agent（例如接收消息监听，发送的消息获取，上下文操作等等）
    - 项目注册管理（项目注册表、增删查）
    - Git版本监控（文件变更追踪、diff快照、版本回退）
    - 广播与公告
- hub层（这里放多设备互通信和相关功能，例如扫描广播房间匹配等等）
    - 设备发现与广播
    - 房间匹配与连接
    - 消息路由与转发（agent消息、系统操作消息、系统通知消息）
    - 故障转移与断线重连
    - 身份标识与权限（名称、描述、设备ID）
- 指令层（这里放所有可以操作系统各个部分的指令）
    - agent指令（创建agent、切换agent、删除agent、重启agent）
    - session指令（创建会话、切换会话、回退上下文）
    - 系统指令（重载配置、重启服务、查看系统状态）
    - 工具指令（启用/禁用工具、修改工具权限）
- cli层（这里放tui-cli客户端的内容，包含UI拓展功能等等）
    - 交互式对话界面
    - 系统状态面板（agent状态、token用量、任务进度）
    - 指令输入与自动补全
    - UI扩展（插件面板、主题配色、多栏布局、通知弹窗）
- web层（这里放web客户端的内容）
    - 核心通信（SSE实时流、REST API、WebSocket、断线重连）
    - 会话界面（对话气泡、流式打字、工具调用卡片、附件上传、消息操作）
    - agent配置面板（API预设管理、模板浏览、工具开关、上下文管理）
    - 系统面板（dashboard总览、实时日志、任务进度、Hub设备列表）
    - 布局与定制（可拖拽面板、暗色亮色主题、响应式设计、快捷键）
- 拓展层（这里放后续更新的官方库与第三方拓展功能）
    - 飞书插件（消息发送、消息回复、消息搜索）
    - 定时器
    - 压力消息系统
    - 桌面宠物
- 其他通用内容
    - log（分级分类结构化）
    - HTTP API接口（供外部系统调用）
    - 错误处理（每层定义自己的错误类型、错误向上转换、用户可见与内部分离）
    - 记录存储（session持久化、审计日志）
    - 配置管理（多层级合并、热重载、项目配置vs用户配置vs默认配置）
    - 安全（权限分级、命令审批流程、敏感信息脱敏）
    - 测试点



   # 待办：
- [ ] 工具
    - [ ] 终端
- [ ] 插件
- [ ] 日志
- [ ] 错误
- [ ] 格式化解析器
- [ ] 测试框架
- [ ] workflow
- [ ] 需求编辑器
- [ ] 

# LLM原始调用层
- 做到：直接可以用最小的代码做成一个完整的chat llm。
## 获取返回原始消息
    - 获取原始信息（流式作流式，非流式直接非流式）
    - 获取封装成非流式的原始消息（流式要转非流式）
## 获取发送出去的消息
    - 获取封装好的完整消息post请求
## 功能类
    - 获取API模型列表
    - api调用测试
    - 设置模型思考强度。
        配置失败，回退，并反馈报错信息。
    - 设置API
        - (添加配置)
            - 配置名称
            - 模型名字列表
            - 模型地址
            - 模型协议
            - 模型上下文
            - 模型Key
        - (删除配置)
            - 配置名称
        - 切换配置
    - 环境变量自动注入 API Key
    - 验证是否符合 schema
    - token费用计算
        - 未命中缓存，命中缓存，输出，缓存率
        - 价格
        - token产值比,时间产值比
    - 事件hook
        - 发送消息
        - 接收消息
            - 模型首字
            - 模型思考开始
            - 模型思考结束
            - 模型输出开始
            - 模型输出结束
            - 解析错误
    - 事件触发
        - 发送消息
            - 文本信息
            - 附件
                - 图片
                - 视频
                - 音频
                - 文档
        - 中断
    - 事件流抽象



# Agent层
# 提示词
    - 格式化提示词
        - 表情
        - 回复
        - 推荐

## agent管理
    - 用agent模板库创建新agent
        - 模板结构体
        - 模板api配置
    - 切换当前agent,使用id或name
    - branch 创建分身（从当前agent创建一个分身，分身的上下文和当前agent相同，但后续独立）
    - commit 合并分身（将分身的上下文合并到主agent）
    - subagent 
    - team
## 结构体
    - content类型
        - 注入历史上下文
        - 透明上下文标注
        - 附件：图片，视频，音频，文档
    - 通用message结构体
        - role
        - content 用户文本结构体或者直接文本
    - 快速用户文本
        - 发送者name身份标识
        - mode：打断，插入（下一轮发送），排队（下一loop）
        - content 用户文本结构体或者直接文本
        - 指令
    - 快速工具回复
        - id：工具id
        - content：工具回复结构体或者直接文本
        - 附件：图片，视频，音频，文档
## agent上下文
    - 消息增量
        - 内容结构体
    - 上下文压缩
        - 提示词
        - 目标
    - 回到上下文xx
    - 回到上下文，并回退。
## 增量注入
    - 注册
    - xml名字
    - 有变更内容时
    - 无变更内容时
    - 烘焙的内容
    - 触发烘焙

## agent状态与事件
    - 单轮状态
        - 发送了user消息
            - 未回复
            - 思考输出中
            - 输出中
        - 未发送user消息
            - 上轮正常
            - 上轮异常
        - 获取当前会话轮次
    - 单轮事件
        - 发送user那一刻
        - 接收消息
            - 模型首字
            - 模型思考开始
            - 模型思考结束
            - 模型输出开始
            - 模型输出结束
            - 解析错误
    - loop状态
        - loop中
        - 获取loop周期
        - 获取该loop调用工具次数
        - 获取该loop当前轮次
    - loop事件
        - loop开始
        - 触发loop第n周期的开始
        - 触发loop第n周期的结束
        - loop结束
    - 服务
        - 任务中
        - 启动中
    - 压缩上下文
        - 压缩前一刻
        - 压缩后一刻
## agent工具
    - bash
    - read
    - write
    - edit
    - task
    - skill
        - 显示路径内skill列表
        - 读取skill成文本
        - 安装skill
        - 卸载skill
        - 获取skill元数据关键词内容
## AGNET对话
    - 发送user消息
        - 结构体必填
            - 发送者name身份标识
            - 发送模式：打断，插入（下一轮发送），排队（下一loop）
            - 附件：图片，视频，音频，文档
            - context结构体数组（或者直接文本）
                - 透明上下文：单次的不被注入上下文的消息
                - 上下文：会注入历史上下文
        - 是否需要保存raw消息：默认是
    - 会话管理
        - 列出所有会话
        - 清空该会话
        - 创建新会话
        - 切换会话
        - fork复制会话
    - 上下文
        - 从session文件加载上下文
        - 读取最近第x条记录（0开始）
        - 读取最近x条记录（1开始）
        - 删除最近第x条记录（1开始）
        - 删除最近x条记录（1开始）
        - 插入记录到指定位置（0开始）
        - 清空所有记录
        - 上下文为保存新session文件
        - 查找
    - 接收消息
        - 获得原始消息（非流式或流式合成结束）
            - 消息.content
            - 消息.思考的内容
            - 消息.命中输入Token数
            - 消息.未命中输入Token数
            - 消息.单次输入Token数
            - 消息.输出Token数
            - 消息.命中缓存的输入文本内容
            - 消息.未命中缓存的输入文本内容
            - 消息.费用
            - 消息.缓存命中率
        - 获取流式累加消息
            - 消息.content
            - 消息.思考的内容
            - 消息.当前获得的总消息json
            - 

## agent-loop修改
    - loop条件接管
        - loop.if（满足内容继续循环）
        - loop.continue（不给内容直接下一轮）
        - loop.return（返回内容给AI，并且下一轮）
            - role
            - content
        - loop.stop（结束循环）

## agent git
    - 变量
        - agent绑定的路径
    - 项目管理
        - git commit 
        - git diff
# Log
    - 显示到日志
        - 分类
        - 头
        - 内容

# 公告栏
    - 增加内容到公告
    - 清空公告栏
    - 删除第x条公告（1开始）

# 插件
    - 诊断
        - 脚本运行消耗时间（默认会显示log）
        - 运行结果返回到log

# cli

# web

# 安全类


# server
    - 启动
    - 停止

# p2p通信
    - 设置广播房间号
    - 扫描广播房间列表
    - 设置身份标识
        - 名称
        - 描述
    - 获取ID
    - 消息类
        - agent消息
        - 系统操作消息
        - 系统通知消息


# 草稿
- 接管loop
    1. 让agent知道你要接管loop
    2. 判断条件（自定义，是否完成某个表格）
        - 条件符合
            3. 等待任务完成
            4. 发送返回数据结构体
        - 条件不符合
            3. 结束循环


- 简单上下文增量
    - 触发发送user消息
        - 自动变成json加入该session的增量
        - 自动完整post日志保存
    - 接收完成消息
        - 自动变成json加入该session的增量
        - 自动完整post日志保存
    - 工具调用结果返回
        - 自动变成json加入该session的增量
        - 自动完整post日志保存
- 手动上下文增量

- 待办栏
    - 扫描
- plan
