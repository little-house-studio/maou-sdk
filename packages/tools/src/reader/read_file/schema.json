{
  "name": "read_file",
  "description": "读取文件内容",
  "parameters": {
    "type": "object",
    "properties": {
      "reason": {
        "type": "string",
        "description": "为什么必须调用此工具？"
      },
      "path": {
        "type": "string",
        "description": "文件路径"
      }
    },
    "required": [
      "reason",
      "path"
    ],
    "additionalProperties": false
  }
}