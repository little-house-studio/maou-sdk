# diff
## 变更感知
- 名单更新：
    - 进入名单：如果使用了阅读/编辑/覆写工具操作了某些文件，那么将会把这些文件放进入该会话的diff监听名单，并开始监听轮次倒数。
    - 退出名单，以下方式其中一种生效即退出监听文件名单：
        - 方式1：如果文件连续200轮没有任何阅读/编辑/覆写该文件将会从名单中移除
        - 方式2：如果已经具有5次更改消息发出去，却一次都没有任何阅读/编辑/覆写该文件，第五次更改消息发送后将会从名单中移除。
- 每次用户与agent发送新的对话时，将会检查名单中的文件的diff，并且传递告诉agent有哪些文件有更改了xx行。
    - 要求较高高性能、低内存消耗
    - 内容仅仅告诉agent距离上一次用户发送消息具有以下文件有改动，并且表明距离上次阅读该文件经历了多久时间，改了的文件列表（为了省token，使用相对距今同路劲），不传递.gitignore中的文件。
    - 传递内容时候不要有那种要让agent一定要去看或者让他去看这些文件的提示词，让他自己决定就行。
        - 最好diff消息也加上提示词告诉agent这个可以不需要查看的情况下可以忽略该消息。
    - diff消息放在before_user中，并且以xml包裹。
- 如果发生这种事情：
    1. 用户更改 -> ai阅读/更改文件A -> 用户更改文件A ->用户和ai发送新的一轮信息<message>。
        - 这种情况，<message>里面会有这个文件diff信息。
    2. 用户更改 -> 用户更改文件A -> ai更改/阅读文件A（更改前会提示并被要求阅读文件） ->用户和ai发送新的一轮信息<message>。
        - 这种情况，<message>里面不会有这个文件diff信息，因为最后更改的时候他已经看过了
## 全局diff
- 继承该跟踪的项目路径内.gitignore的排除配置

# 问题待办
- [ ] 如何设置api？
    - [ ] 配置文件在哪？如何做后面大系统的兼容？是否有一个独立的Agent文件？
- [ ] 功能完善吗？
    - [ ] tui
- [ ] 满足以下需求了吗？
    - [ ] 自动LSP
    - [ ] 记忆点持久化
    - [ ] 上下文压缩
        - [ ] 微压缩
        - [ ] 大压缩
            - [ ] 会话归档
    - [ ] 工具调用
        - [ ] 使用skill
        - [ ] 调用网页搜索subagent
        - [ ] 快速网页搜索
        - [ ] 专门搜索代码结构
        - [ ] 搜索文件名
        - [ ] 搜索文件内内容
        - [ ] 阅读工具
        - [ ] 覆写文件工具
        - [ ] 编辑文件工具
        - [ ] 终端工具
        - [ ] 管理终端工具
        - [ ] 待办清单工具
        - [ ] subagent管理和沟通
        - [ ] 公告栏工具（记忆工具）
    - [ ] 子agent
        - [ ] 网页搜索agent
        - [ ] goal模式的监督agent
    - [ ] 文件烘焙
    - [ ] 项目资料文件
        - 增加/init 指令：
            - 文件：<project>/.maou/project/
                - USER.md：用户相关的偏好
                    - 称呼 / 角色（开发者？产品？）
                    - 沟通偏好（语言、emoji、要不要废话、是否要先方案后动手）
                    - 工作习惯（爱用 monorepo？先写测试？）
                    - 明确讨厌的事（「别堆 file:line」「别擅自 commit」）
                - PROJECT.md：项目相关的
                    - 一句话定位（产品是干什么的）
                    - 核心模块 / 目录地图（相对完整和总要与大纲 tree，是「去哪找什么」）
                    - 技术栈与关键依赖
                    - 怎么跑：dev / build / test / typecheck
                    - 关键数据流 / 架构要点（3～8 条就够）
                    - 和兄弟项目或者依赖的关系（如果有）
                    - 其他重要事项
                - RULE.md：项目相关的规则
                    - 代码风格
                    - 不硬编码
                    - 禁止操作（别 commit .maou/config.json、别动 secrets）
                    - 必须走的流程（改 SDK 要 rebuild、PR 前 typecheck）
                    - 安全边界
                    - 测试要求
                - DESIGN.md：项目相关的设计偏好（ADR 精简版）
                    - 设计原则
                    - 设计模式
                    - 设计目的
                - EXPERIENCE.md：在项目中研究到的相关经验
                    - 反直觉的、之前踩过的坑
                    - 有关环境的问题
                    - 绝对不要做的事情
                    - 建议不要做的事情
                - ...
            - 这几个文件会传递到所有有关这个项目的主agent和子agent，嵌入到系统提示词中。
            - 这个init的活是由主agent干的，因为主ganet接触到的用户数据最多。
            - 流程：
                1. 发送指令后，识别成功后，会类似读取skill的方式发任务和提示词，注入详细的init要求提示词去扫描项目有关内容。
                    - 嵌入指令的提示词内容包括
                        - 让他这次他的任务是什么
                        - 告诉他<project>/.maou/project/里面的5个文档分别是写什么内容，建议先读再去写。
                        - 完整独立告诉他这5个文档的具体需求和模板框架   
                        - 告诉他，并且规范他可以阅读所有内容的不能写别的内容，只能写<project>/.maou/project/里面的文件。（包含终端指令过滤）
                        - 告诉他，他的工作流程：先读所有自己需要的文件信息，再写到5个文档里面。
                        - 不能做的事情：
                            - 把密码、敏感信息等内容明文写到里面
                2. ai阅读完成后会询问用户5-20个重要问题，用户回答后
                3. 用户回答完成后开始撰写<project>/.maou/project/里面的全部文件，按需填写。
            - 注入：
                - 5个文件被xml包住表明为项目信息，注入到系统提示词中，并且在破坏上下文时机更新这个注入部分。（这里有相关底层逻辑配置，不重写逻辑）
    - [ ] 内容总结
    - [x] diff感知（agent 层 FileDiffWatch；coding-agent 默认 fileDiffWatch:true）
    - [ ] 输入框内容自动补全
    - [ ] 安全批准和名单
    - [ ] 故障处理与重试