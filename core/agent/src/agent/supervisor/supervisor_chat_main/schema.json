{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "supervisor_chat_main",
  "description": "监督 Agent → 主 Agent 沟通工具（仅监督模式下可用）",
  "type": "object",
  "properties": {
    "message": {
      "type": "string",
      "description": "发给主 Agent 的指令/消息"
    },
    "wait": {
      "type": "boolean",
      "description": "是否等待主 Agent 完成（默认 true）"
    },
    "description": {
      "type": "string",
      "description": "一句话说明这次调用在做什么（任务简介；CLI 折叠标题只显示此项）。"
    }
  },
  "required": [
    "description",
    "message"
  ],
  "additionalProperties": false
}
