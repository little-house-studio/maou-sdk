## 使用指引

- 不传 path 撤销最近一次任意文件的编辑；传 path 撤销指定文件最近一次编辑。
- all=true 会撤销该 session 全部历史编辑（按时间倒序），慎用。
- undo_edit 只能撤销 edit_file 和 write_file 的改动，不能撤销终端命令产生的文件变更。
- 撤销后建议用阅读工具确认文件恢复到了预期状态。
- 如果撤销操作本身也出了问题，可以再次调用 undo_edit 回退。
