## 使用指引

- 写入前先确认目标路径是否正确，避免覆盖重要文件。
- content 必须是完整的文件内容，不是增量或片段。每次调用会整体替换文件。
- 自动创建不存在的父目录，无需手动 mkdir。
- 大文件写入后建议用 read_file 抽查关键行，确认写入正确。
- 不要为了"记录"或"备忘"而写文件，只在用户要求或任务需要时使用。
- 修改已有文件的小部分内容时，优先使用 edit_file 而非 write_file，避免意外丢失其他内容。
