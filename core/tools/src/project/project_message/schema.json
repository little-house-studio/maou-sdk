{
  "name": "project_message",
  "description": "发送消息给项目agent（占位）",
  "parameters": {
    "type": "object",
    "properties": {
      "reason": {
        "type": "string",
        "description": "为什么必须调用此工具？"
      },
      "to": {
        "type": "string",
        "description": "目标agent"
      },
      "content": {
        "type": "string",
        "description": "消息内容"
      }
    },
    "required": [
      "reason",
      "to",
      "content"
    ],
    "additionalProperties": false
  }
}