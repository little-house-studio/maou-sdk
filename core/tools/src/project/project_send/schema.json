{
  "name": "project_send",
  "description": "向已注册项目的 Coding Agent 派发完整任务。不绑定路径。失效时按返回提示用 project_agent create。",
  "parameters": {
    "type": "object",
    "properties": {
      "project": {
        "type": "string",
        "description": "项目绝对路径或唯一项目名"
      },
      "task": {
        "type": "string",
        "description": "发给项目 Agent 的完整任务说明"
      },
      "description": {
        "type": "string",
        "description": "一句话说明这次调用在做什么（任务简介；CLI 折叠标题只显示此项）。"
      }
    },
    "required": ["description", "project", "task"],
    "additionalProperties": false
  }
}
