# submit_plan

Submit a complete markdown implementation plan for user review.

The plan must start with a `#` heading and be decision-complete: intent, settled decisions, steps, verification, non-goals, and risks. Do not implement after submitting. The user will `/plan approve`, `/plan revise <notes>`, or `/plan off`.
