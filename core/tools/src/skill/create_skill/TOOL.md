## 使用指引

- 创建新的 skill（专业知识包）。name 使用 kebab-case 格式，如 code-review、lark-im。
- requirements 是关键参数，需详细说明：目的、大纲、验收标准、是否需要程序代码资源、CLI 工具或外部依赖。
- requirements 长度 50-3000 字，写得太简单会导致生成的 skill 质量差。
- 创建后会生成 SKILL.md 模板，按引导完善和测试。
- 只在需要创建新的可复用知识包时使用，一次性任务不需要创建 skill。
