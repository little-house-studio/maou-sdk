## 使用指引

- mode=search：搜索 skills.sh 上的 skill，按安装量排序返回 top 20。
- mode=install：下载 skill 到当前 agent 的 skills/ 目录。
- 搜索语法：多关键词空格分隔（AND 逻辑），owner:xxx 限定发布者，repo:xxx 限定仓库。
- 安装格式：owner/repo@skill 或 GitHub URL。
- 安装后 skill 自动可用，无需额外配置。
- 不确定有哪些 skill 可用时先搜索，再按需安装。
