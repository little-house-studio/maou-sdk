# create_goal

Create one persisted `/goal` completion contract from a direct human request. Do not use this for trivial single-turn work. Subagents cannot create a goal. Disabled while a host-verified `/ultragoal` is open.
