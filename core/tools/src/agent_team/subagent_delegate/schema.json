{
  "name": "subagent_delegate",
  "description": "委托任务给子 Agent。子 Agent 是独立配置的 Agent（文件即子 Agent 约定），在独立 session 中执行任务并返回输出。仅在需要委派给专用子 Agent 时使用。",
  "parameters": {
    "type": "object",
    "properties": {
      "task": {
        "type": "string",
        "description": "要委托给子 Agent 的任务描述（自然语言）。"
      }
    },
    "required": [
      "task"
    ],
    "additionalProperties": false
  }
}
