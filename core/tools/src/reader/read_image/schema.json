{
  "name": "read_image",
  "description": "读取本地图片（png/jpg/gif/webp 等），以多模态返回。",
  "parameters": {
    "type": "object",
    "properties": {
      "path": { "type": "string", "description": "本地图片路径" },
      "description": { "type": "string", "description": "一句话说明这次调用在做什么（任务简介；CLI 折叠标题只显示此项）。" }
    },
    "required": ["description", "path"],
    "additionalProperties": false
  }
}
