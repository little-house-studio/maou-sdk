## 使用指引

- pattern 支持标准 glob 语法：`*` 匹配文件名、`**` 递归目录、`?` 单字符、`[abc]` 字符集。
- 常用模式：`**/*.ts`（所有 TS 文件）、`src/**/*.py`（src 下所有 Python 文件）、`*config*`（含 config 的文件名）。
- 不确定文件名时先用宽泛模式搜索，再缩小范围。
- head_limit 默认 100，文件很多时适当调大，但不要超过 500。
- glob 只按文件名匹配，不搜文件内容。搜内容用 grep。
