{
  "name": "control_terminal",
  "description": "控制终端操作（占位）",
  "parameters": {
    "type": "object",
    "properties": {
      "reason": {
        "type": "string",
        "description": "为什么必须调用此工具？"
      }
    },
    "required": [
      "reason"
    ],
    "additionalProperties": false
  }
}