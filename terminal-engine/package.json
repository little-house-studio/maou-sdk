{
  "name": "@little-house-studio/terminal-engine",
  "version": "0.1.0",
  "description": "高性能终端引擎 — Rust + napi-rs",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "name": "terminal-engine",
    "triples": {
      "defaults": true
    }
  },
  "scripts": {
    "build": "node -e \"if(require('fs').existsSync('./index.js')){console.log('skip napi build - using JS mock');}else{process.exit(1);}\" || napi build --release --platform",
    "build:debug": "napi build --platform",
    "prepublishOnly": "napi prepublish -t npm"
  },
  "files": ["index.js", "index.d.ts", "*.node"],
  "license": "MIT"
}
