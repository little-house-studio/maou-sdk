# @little-house-studio/install-agent

安装员：无 TUI。把托管仓库装到本机，或检修运行时。

```bash
npm i -g @little-house-studio/install-agent
aiinstall <链接或需求>
```

最低要求：Node >= 20 + 已有 API（复用 `~/.maou/config.json`）。强制 mini 终端，默认 AUTO 审批。
