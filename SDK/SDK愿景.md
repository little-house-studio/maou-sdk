# SDK愿景
- 案例目标：
    - 只要llm+context层+cli就能不到1000行代码构建一个完整的带群聊酒馆。
        - 支持单聊
        - 支持群聊
        - 支持ai输出结构化格式（输出json）
        - 支持自定义角色卡
    - 只要一个llm+context层不到300行代码构成一个完整的chat app
    - 只要一个agent层+cli层，不到1000行代码就可以构建一个极简的pi agent.
    - 只要llm层完成一个浏览器内男女对立评论屏蔽插件
