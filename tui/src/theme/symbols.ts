// ── 装饰符号（与 cli/theme/tokens SYMBOLS 对齐） ─────────────────────
//
// 纯常量。spinner 从 symbolTheme.spinnerFrames 取（与 Pi Loader 一致，不在此重复定义）。

export const SYM = {
  separator: "//",
  index: "▌",
  marker: "▸",
  recDot: "●",
};
